name: Deploy demo to Pages

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/pages.yml'
      - 'docs/demo/**'
      - 'scripts/generate-demo.ts'
      - 'scripts/build-demo.ts'
      - 'scripts/build-world.ts'
      - 'scripts/build-tour.ts'
      - 'scripts/generate-catalog.ts'
      - 'scripts/catalog/**'
      - 'src/**'
      - 'package-lock.json'
      - 'package.json'
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          node-version: 24
          cache: npm
      - run: npm ci
      - run: npm run generate:demo
      - run: npm run generate:catalog
      - run: npm run check:catalog
      - name: Verify enhanced demo and static fallback assets
        run: |
          test -s docs/demo/app/main.js
          test -s docs/demo/world/index.html
          test -s docs/demo/world/app/main.js
          test -s docs/demo/tour/index.html
          test -s docs/demo/tour/app/main.js
          test -s docs/demo/tour/credits.html
          test -s docs/demo/tour/credits.css
          test -s docs/demo/tour/models/manifest.json
          test -s docs/demo/tour/models/CREDITS.md
          test -s docs/demo/tour/models/licenses/CC0-1.0.txt
          test -s docs/demo/tour/models/licenses/CC-BY-3.0.txt
          test -s docs/demo/tour/models/licenses/Quaternius-ultimate-pack.txt
          for animal in squirrel cow deer fox horse donkey sheep pig; do
            test -s "docs/demo/tour/models/$animal.glb"
          done
          test -s docs/demo/catalog/index.html
          test -s docs/demo/catalog/catalog.json
          for preset in nature balanced civilization; do
            for mode in dark light; do
              test -s "docs/demo/assets/preset-$preset-$mode.svg"
            done
          done
      - uses: actions/configure-pages@v6
      - uses: actions/upload-pages-artifact@v5
        with:
          path: docs/demo
      - id: deployment
        uses: actions/deploy-pages@v5
